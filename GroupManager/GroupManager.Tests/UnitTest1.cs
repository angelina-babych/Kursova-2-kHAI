using BusinessLogic.Repositories;
using GroupManager.Core.Context;
using GroupManager.Core.Model;
using GroupManager.ViewModels;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.ComponentModel;
using System.Threading.Tasks;

namespace GroupManager.Tests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestAddStudent()
        {
            //GroupRepository studentsRepo = new GroupRepository(new ApplicationContext());
            //Group group = new Group { Id = Guid.NewGuid(), Name = "ssasdadssasa" };
            //studentsRepo.Add(group);
            //var expected = false;

            ////Act
            //var actual = studentsRepo.Add(group);
            //Task.Delay(2950).Wait();
            ////Assert
            //Assert.AreEqual(true, true);
            //StudentRepository studentsRepo = new StudentRepository(new ApplicationContext());
            //Student stud = new Student {Id=Guid.NewGuid(), GroupId = new Guid("F6B44AE0-6F7E-4582-932F-5740F6E08E39") };
            //studentsRepo.Add(stud);

            //var expected = false;

            ////Act
            //var actual = studentsRepo.Add(stud);

            ////Assert
            //Assert.AreEqual(expected, actual);
        }
    }
}
